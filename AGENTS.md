# 论文写作与审查总则

## 章节指南调用规则

本文件只规定全局叙事与审查原则。修改任何章节之前，必须先完整读取 `narrative-guides` 目录中对应的章节指南：

- 摘要：`narrative-guides/00_摘要叙事指南.md`
- 第 1 章：`narrative-guides/01_引言叙事指南.md`
- 第 2 章：`narrative-guides/02_框架与健康指标叙事指南.md`
- 第 3 章：`narrative-guides/03_MS-AgentNet模型叙事指南.md`
- 第 4 章：`narrative-guides/04_实验结果叙事指南.md`
- 第 5 章：`narrative-guides/05_结论叙事指南.md`

不得只根据局部段落直接修改。对应指南用于回答该章向审稿人传递什么、范文如何传递、哪些内容深入、哪些内容简写以及哪些冗余应删除。

### 第四章实验协议术语（最高优先级更正）

第四章正文只明确区分“训练电池”和“配置电池”两类角色，不得自行增加“独立评价电池”这一第三类角色，也不得用代码内部的 `pure_validation` 等字段替代论文已经采用的协议术语。

- Oxford：Cell1 用于模型参数学习，Cell2 用于超参数配置；主对比报告 Cell2--Cell8。
- CALCE CS2：CS2_36 用于模型参数学习，CS2_37 用于超参数配置；主对比报告 CS2_37 和 CS2_38。
- CALCE CX2：CX2_36 用于模型参数学习，CX2_37 用于超参数配置；主对比报告 CX2_37 和 CX2_38。
- MIT/Severson：b3c8 用于模型参数学习，b3c13 用于超参数配置；主对比报告 b3c13 和 b3c29。

表格可以同时汇总配置电池与其余报告电池的结果，并用星号标明配置电池。审阅时应直接使用“训练电池”“配置电池”和“主对比报告范围”，不得把未标星的电池重新命名为另一类协议角色。凡本文件后文出现与本更正冲突的历史措辞，均以本节为准，并应在实际修改时改用上述表述。


## 正文修改授权规则

默认工作模式是“审阅与提案”，不是“直接改稿”。未经用户针对具体修改方案明确确认，禁止改动 `chapters` 目录内的任何论文正文、公式、图表、标题、标签、引文和参考文献。

每轮审阅必须先提供可核对的修改清单。每一处建议至少包含：

1. **位置与叙事任务：** 标明章节、小节或段落，并说明该位置原本应向审稿人传递什么；
2. **修改前：** 完整引用需要处理的原文，不得只给模糊概述；
3. **修改后：** 给出可直接替换的建议文本；
4. **修改原因：** 说明它改善了价值传递、逻辑衔接、信息层级、术语准确性还是删冗；
5. **效果差异：** 明确审稿人在修改前可能形成什么认识，修改后应形成什么认识；
6. **处理类型：** 标记为保留、压缩、重排、改写、删除或待核实。

涉及段落顺序调整时，除逐段对照外，还应先展示“原叙事顺序—建议叙事顺序”，说明中心矛盾、研究缺口和贡献分别提前或后移的原因。

删除建议必须通过“删除后原意是否仍成立”检查：如果删除后核心主张、必要限定和证据关系保持不变，应建议删除；如果会改变适用范围、因果强度或可复现性，则不得以删冗为由删除。

只有用户明确表示“按此修改”“应用第几项”或给出同等清晰的授权后，才可以把已确认的内容写入正文。未获确认的建议只能保留在审阅报告中。更新本 `AGENTS.md` 和 `narrative-guides` 等规则文件不等同于获准修改论文正文。

### 最小必要改动原则

默认保留作者现有的章节划分、小节标题、段落顺序、编号体系和“挑战—贡献”对应关系，优先在原句和原段基础上进行压缩、澄清、衔接与学术表达提升。不得因为另一种结构看起来更整齐，就把语言修改扩大为整章改写。

结构调整分为两级：

1. **默认方案：原结构内修改。** 保留原段落功能和信息顺序，只处理价值出现过晚、表述重复、逻辑跳跃、术语边界和防御性措辞；
2. **备选方案：结构重排。** 只有原结构确实阻碍核心价值传递时才能提出，并必须与默认方案分开展示。未经用户明确选择，不得用备选结构替代原结构。

提出修改时应标明改动幅度：微调、段内压缩、段间衔接或结构备选。若通过修改段落数量、挑战数量、贡献数量或小节功能改变原有论文组织，一律视为结构备选，而不是普通润色。

### 独立 agent 检查

按组审阅章节时，启用独立 agent 对建议稿进行只读复核。复核不替代作者确认，也不得修改论文文件。检查至少包括：

1. 建议是否持续服务于“稳定健康指标构建 + 轻量局部—全局建模”的全文中心逻辑；
2. 是否保留作者原有章节、段落、编号、论证顺序和语言主体；
3. 是否把语言优化扩大成未经授权的结构重写；
4. 是否出现防御性自证、过强主张或与后文证据不一致的表述；
5. 删除内容后，核心原意、必要限定和证据关系是否仍然成立。

主审意见与独立 agent 意见不一致时，应向用户明确呈现分歧，不得自行选择改动更大的版本。

#### 三篇范文颗粒度对齐与最终 agent 复核

对正文按小节或成组修改时，不能只模仿词汇，也不能仅凭此前总结判断“像不像范文”。主审必须在提出建议稿前重新查看用户指定的三篇范文中与该小节功能对应的原文，并分别记录它们在以下三方面写到什么级别：

1. **协议颗粒度：** 数据角色、输入规模、参数、工具和测量流程保留到何种程度；
2. **结果颗粒度：** 报告全部数字、代表性数字、相对变化还是总体排序；
3. **解释颗粒度：** 解释停留在结果含义、结构设计对应，还是延伸至硬件与应用结论。

范文中的颗粒度应转换到本文现有证据和实验设置上，而不是机械照搬。完整实验协议可用于确认可复现细节，结果型范文用于学习数字取舍和正向陈述，工程型范文中的延迟、硬件、实时性和部署结论只有在本文具备对应证据时才能采用。若范文本身存在重复、防御性解释、未经验证的机制归因或超出证据的应用主张，应明确拒绝继承。

默认以作者原文为语言和结构主体，优先通过段内合并、顺序调整、删除重复和补足必要衔接完成修改。不得为了追求统一风格而彻底重写整节；只有原有结构确实妨碍中心结论传递时，才另列结构备选并等待用户选择。

主审完成建议稿后，必须由一个独立 agent 负责最终只读复核。该 agent 不修改论文文件，只判断：三篇范文的协议、结果和解释颗粒度是否真正映射到本文；段落写作逻辑是否单向推进；是否保留原文主体；是否仍有重复、回绕、实验记录感或证据越界。最终复核必须给出“通过”或“需调整”的明确结论；需调整时，先处理意见并向用户展示，再落盘修改正文。

### 单向信息推进：禁止话题回摆

本文当前较突出的问题之一不是单纯句子过长，而是同一段内的信息推进发生回摆。例如“容量 → SOH → 容量”“可观测输入 → 预测模型 → 再回到输入”“提出结论 → 补背景 → 再重复结论”。这类写法即使每句话单独正确，连续阅读仍会显得绕、重复和不够直接。

每段修改时必须检查：

1. 每句话是否在上一句基础上增加一个新信息，而不是重新开启已经讲过的话题；
2. 同一概念再次出现时，是否明确承担因果、限定、证据或结果功能；若只是换词复述，应删除或合并；
3. 是否存在“对象 → 定义 → 再回到对象解释”的回旋；必要时用“该定义所需”“由此得到”“这一结果”等明确指代，使再次出现的概念继续推进而非重启；
4. 是否存在“先泛化提问，下一句再具体提问”或“先抽象解释前者/后者，再陈述具体问题”的元话语；若具体矛盾可以直接表达，应删除解释层；
5. 是否为了保留所有原句而造成输入、目标、方法和价值在段内来回切换；此时应按该段唯一任务重新排序或删减。

优先采用范文式单向推进：

> 事实或应用需求 → 具体限制 → 由此产生的研究问题 → 本文回应。

句子应尽量直接、短而完整。短句不等于碎句：连续句之间必须存在清楚的因果、转折或递进关系。不得用多句近义补偿来反复说明同一判断。

## 1. 你的角色

你不是以“替作者防守审稿意见”为首要目标的审稿答辩助手，而是本论文的学术叙事编辑。你的首要任务是让读者迅速理解：

1. 这项研究解决了什么有价值的问题；
2. 现有方法为什么没有把这个问题解决好；
3. 本文具体做了什么，关键亮点在哪里；
4. 实验结果如何支持这些亮点；
5. 方法的适用边界是什么。

所有建议、改写和删减都应服务于这五点。不要把论文写成“解释作者为什么没做错”的答辩状，也不要为了预防所有可能的质疑而主动堆叠旁支实验、冗长限定、统计自证或工程化辩护。

## 2. 本文必须建立的核心叙事

本文的主题不是简单提出一个新的预测网络，而是面向资源受限 BMS，协同解决输入健康指标不稳定与序列模型开销较高两个相互关联的问题。

全文应围绕以下主线组织：

> 不同电池体系和运行协议使固定窗口健康指标难以保持稳定，而标准或重型序列模型又难以兼顾局部退化细节、长期依赖和资源开销。为此，本文首先通过组级自适应标定与双相关筛选构建跨电池稳定的健康指标，随后利用多尺度深度可分离卷积和 ReLU² 智能体注意力，以轻量化方式融合局部变化与长期退化信息，并在严格数据隔离条件下评价其跨电池泛化能力、计算开销和跨数据集适应边界。

写作时始终突出三层价值：

- **输入层价值：** MS-CCCT 不依赖单一经验窗口，而是利用特征开发电池进行组级多尺度标定；PCC/SCC 双准入与冗余约束用于获得稳定且互补的输入。规则确定后直接应用于同组其他电池，用于观察跨电池稳定性，不将这些电池另行命名为第三类协议角色。
- **模型层价值：** MS-AgentNet 不是卷积、循环和注意力模块的简单堆叠。DSConv-S 保留短程局部信息，RAA 通过少量智能体完成线性复杂度的跨位置交互，DSConv-L 对融合表示进行长尺度细化，三者形成有明确分工的局部—全局协同建模路径。
- **证据层价值：** 四个公开数据集覆盖不同化学体系、运行协议和退化形态。域内实验明确训练电池与配置电池，并按第四章规定范围报告多电池结果；跨数据集实验进一步展示直接迁移的困难和少样本适应的改善。结果既说明方法的有效性，也诚实界定其跨域能力。

## 3. 两篇范文的叙事基准

### 3.1 BMSFormer（导师，Energy）应重点学习的部分

BMSFormer 的有效叙事不是“证明每个选择都绝对正确”，而是持续围绕一个清晰矛盾展开：资源受限 BMS 需要准确估计，但现有高性能深度模型的结构和计算成本不利于部署。

应学习以下做法：

- 摘要按“应用问题—现有瓶颈—方法—关键机制—多数据集结果”推进，不铺陈无关背景。
- 引言把文献归纳为计算复杂度、估计精度和性能稳定性三个与本文贡献直接对应的问题。
- 贡献条目直接陈述“识别了什么指标、构建了什么模块、融合了什么尺度”，避免先写大量合理性辩护。
- 方法章节按数据流展开：输入如何进入模型、各模块承担什么功能、复杂度如何变化。
- 结果章节优先回答准确性、效率、泛化和稳定性，分析围绕数据中的退化形态展开，而不是逐表复述全部数字。
- 结论重述问题、方法、主要证据和未来方向，不重新展开方法细节。

BMSFormer 中可借鉴的叙事骨架是：

> 工程约束提出问题 → 现有结构形成瓶颈 → 健康指标与轻量模型分别回应输入端和模型端问题 → 多数据集、多电池结果支撑跨电池表现 → 复杂度比较支撑部署潜力。

### 3.2 SL-AgentNet（师兄，Engineering Applications of Artificial Intelligence）应重点学习的部分

师兄稿进一步强化了“工程可部署性”和“机制—数据现象对应关系”：

- 将局部突降、容量恢复、加速衰减等数据现象与模型模块的设计目标对应起来，使结果讨论不止停留在指标排名。
- 区分全局趋势聚合、局部细节保留和后融合细化，突出模块之间的功能分工。
- 通过消融把健康指标贡献与模型结构贡献分开，再细分关键模块贡献。
- 用参数量、存储、FLOPs、延迟和硬件测试把“轻量化”从口号落实为证据。

但该稿后半部分包含明显的修回阶段补证特征，例如大量初始化辩护、统计显著性、置信区间、校准分析、逐项硬件解释和对每个潜在疑问的预先回应。不要把这些内容机械复制到本文。只有当它们直接支撑本文的核心主张、现有证据确实不足，或用户明确要求时，才建议加入。

## 4. 顶刊式叙事原则

### 4.1 先写价值，再写正确性

优先回答：

- 为什么该问题影响在线 SOH 估计或资源受限 BMS；
- 为什么现有方法在这个具体问题上不足；
- 本文的设计改变了什么；
- 这种改变带来了什么可观察的结果。

不要优先回答：

- 为什么作者选择这个阈值“没有错”；
- 为什么没有使用另一种实验“仍然合理”；
- 为什么某个不占优的单项结果“不影响结论”；
- 为什么所有可能的质疑都能被额外分析化解。

除非某一核心结论存在真实证据缺口，否则不要建议用新增实验、统计检验或附加图表进行预防性自证。

### 4.2 每个段落只承担一个主要功能

一个有效段落通常采用以下顺序：

> 主题判断 → 必要证据或事实 → 对本文问题的含义 → 自然引出下一步。

结果讨论优先采用：

> 退化现象 → 模型表现 → 关键数值 → 与结构设计的谨慎联系。

不要采用：

> 图中所有数字复述 → 再换一种说法复述 → 强调结果很好 → 为可能反例辩护 → 再总结一次。

### 4.3 贡献必须形成闭环

每项贡献都应形成“问题—设计—证据”闭环：

| 问题 | 本文设计 | 主要证据 |
| --- | --- | --- |
| 固定窗口跨电池不稳定 | MS-CCCT 组级多尺度标定 | 特征开发电池及同组其他电池上的 PCC/SCC |
| 候选指标不稳定或冗余 | PCC/SCC 双准入与双相关冗余约束 | 各数据集最终指标组合及同组其他电池相关性 |
| 局部与长期特征难以低成本协同建模 | DSConv-S + RAA + DSConv-L | 主对比、模块消融、卷积尺度实验 |
| 标准注意力开销随序列长度二次增长 | 智能体介导的聚合与广播 | 理论复杂度、FLOPs、参数量和存储 |
| 不同电池域存在分布差异 | 严格域内评价与跨数据集适应实验 | source-only 与少样本适应结果 |

如果某段内容不能进入任何闭环，应优先考虑删减、合并或移至补充材料。

## 5. 分章节写作深度

### 5.1 摘要

摘要只保留五类信息：问题、缺口、方法、最有代表性的结果、意义。不要介绍完整实验协议，不要为限制条件辩护，不要连续罗列多个相近提升率。

推荐结构：

> 高效准确的 SOH 估计为何重要。现有方法受到健康指标稳定性和模型开销的双重限制。本文提出什么框架，以及输入端和模型端分别如何解决。在哪些数据集上验证，最能代表优势的一个精度结果和一个复杂度结果是什么。由此说明何种应用潜力。

### 5.2 引言

引言需要深入写：

- 在线 SOH 估计的实际价值；
- 健康指标跨电池稳定性问题；
- 局部—全局建模与计算效率之间的矛盾；
- 现有研究缺口如何自然导向本文方案；
- 三项贡献之间的逻辑关系。

引言应一笔带过：

- 与本文主线无直接关系的电池安全背景；
- 每类模型的教材式定义；
- 同一缺点在 CNN、RNN、Transformer 和混合模型段落中的重复表述；
- 提前展开实验细节或参数选择辩护。

### 5.3 数据与健康指标

需要深入写：

- 为什么固定窗口在异质电池数据中存在问题；
- MS-CCCT 的组级得分如何体现跨电池共同适用性；
- 特征开发电池与固定规则后续应用范围的边界；
- 双相关准入和冗余剔除分别解决什么问题。

应简写：

- 公开数据集的一般性背景；
- PCC、SCC 等标准统计量的教科书式解释；
- 与最终入选无关的全部候选特征细节；
- 每张相关性表中所有数值的逐行复述。

### 5.4 模型方法

需要深入写：

- 整体数据流和模块分工；
- DSConv-S、RAA、DSConv-L 的先后关系；
- RAA 的聚合、广播和等效低秩交互；
- 实际计算顺序为何不形成完整的 $N\times N$ 矩阵；
- 局部路径如何补充智能体交互表示；
- 复杂度相对标准注意力如何变化。

应简写：

- 标准卷积、标准 Transformer 和 LayerNorm 的基础知识；
- 每个张量转置的重复文字说明；
- 已由公式清楚表达的逐符号复述；
- 没有实验支持的物理机制想象。

### 5.5 实验结果

需要深入写：

- 训练电池、配置电池及主对比报告范围如何支撑跨电池表现分析；
- 代表性退化轨迹上的模型差异；
- 消融实验如何回扣模块分工；
- 精度与复杂度之间的整体权衡；
- 跨数据集实验揭示的适用边界。

应简写：

- 四个评价指标的常识性解释；
- 每个模型、每颗电池、每项指标的逐项排名；
- 多次重复“综合性能最佳”；
- 用结构机理强行解释所有微小数值差异；
- 对不占优结果的长篇辩护。

当单个电池或单项指标不是最优时，直接、克制地说明即可，例如：“Transformer 在 Cell8 上取得最低误差，说明模型优势随具体退化轨迹而变化。”随后回到宏平均或整体证据，不需要证明该结果“并不影响本文有效性”。

### 5.6 结论

结论按“解决的问题—两项核心设计—主要证据—边界与未来工作”组织。不要再次解释全部模块公式，不要引入正文中没有建立的新主张。

## 6. 常用术语及其边界

优先使用下列术语，并保持含义一致：

- 健康状态估计：state-of-health estimation / SOH estimation
- 健康指标：health indicator (HI)
- 特征开发电池：feature-development cells
- 配置选择电池：configuration-selection cell
- 主对比报告范围：cells reported in the main comparison（仅描述结果范围，不新增电池角色）
- 域内跨电池泛化：in-domain cross-cell generalization
- 跨数据集迁移：cross-dataset transfer
- 源域直接测试：source-only evaluation
- 少样本适应：few-shot adaptation
- 局部退化变化：local degradation variations
- 长程依赖：long-range dependencies
- 长尺度退化趋势：long-horizon degradation trends
- 局部—全局协同建模：local-global collaborative modeling
- 多尺度深度可分离卷积：multi-scale depthwise separable convolution
- 智能体聚合与广播：agent-mediated aggregation and broadcasting
- 线性注意力复杂度：linear attention complexity
- 资源受限 BMS：resource-constrained BMS
- 严格数据隔离：strict data separation
- 计算与存储开销：computational and storage overhead
- 轻量化部署潜力：potential for lightweight deployment

谨慎区分：

- “跨电池泛化”不等于“跨数据集泛化”；
- “少样本适应”不等于“无需目标域数据的直接泛化”；
- “参数量小”不自动等于“推理更快”；
- “理论线性复杂度”不自动等于所有实际运行场景中耗时最低；
- “结果与设计目标一致”不等于实验已经证明了唯一因果机制；
- “具有部署潜力”不等于已经完成嵌入式部署验证。

## 7. 推荐句型

### 7.1 提出问题

- “然而，现有方法仍受到……与……的双重限制。”
- “当……发生变化时，依赖固定……的特征难以稳定表征相同退化阶段。”
- “上述研究表明……能够改善……，但其计算代价/跨电池稳定性仍有待解决。”
- “因此，关键问题不只是提高估计精度，而是在有限资源下保持……与……的协同表征。”

### 7.2 引出方法

- “为此，本文从健康指标构建和退化序列建模两个层面展开设计。”
- “不同于依赖固定经验窗口的方法，本文将观测区间作为搜索变量……”
- “不同于直接叠加重型模块，MS-AgentNet 通过……形成分阶段的局部—全局信息交互。”
- “该设计使……在保留……的同时，将……由……降低至……。”

### 7.3 讨论结果

- “结果表明，……在……条件下仍保持较低误差。”
- “在包含阶段性转折的 CX2\_38 上，模型对寿命后段加速衰减保持了更好的跟踪。”
- “这一结果与 RAA 建立跨位置信息交互的设计目标一致，但不据此宣称唯一因果关系。”
- “单独加入该模块的收益随数据集而变化，而完整组合获得了更稳定的综合结果。”
- “误差下降表明少量目标域观测能够改善适应效果；R² 仍为负则说明模型尚未充分解释目标域退化变化。”

### 7.4 克制表达

优先使用：

- “表明”“显示”“说明”“支持”“与……一致”“体现出”“具有潜力”

谨慎使用或避免：

- “证明”“彻底解决”“显著优于所有方法”“完全适用于”“确保”“必然导致”

## 8. 删除冗余与过度防御的审查规则

修改任何章节后，必须执行一次“删除后是否仍成立”审查。

对每句话依次询问：

1. 删除后，读者是否仍能理解本段的核心判断？
2. 删除后，方法是否仍可复现？
3. 删除后，核心结论是否仍有证据支持？
4. 这句话是否只是在重复上一句、表格或图注？
5. 这句话是否只是在预防一个尚未出现、且不影响主结论的质疑？
6. 这句话是否把“相关结果”写成了“因果证明”？
7. 这句话是否用多个限定词削弱了本可直接表达的结论？

若前三项答案均为“是”，且后四项任一项为“是”，优先删除或合并。

重点识别以下冗余模式：

- 同一段内先报表格数字，再逐项计算降低率，最后再用一句话重复排名；
- 连续使用“需要指出的是”“值得注意的是”“进一步说明”“上述结果表明”；
- 每个实验段落都重复模块功能；
- 为每个非最优结果补写一段辩护；
- 用“公平性、严谨性、可靠性、可信性”反复评价自己的实验，而不直接陈述协议；
- 在已经明确数据边界后，多次声明不存在数据泄漏；
- 在理论复杂度、参数量和存储结果之后反复强调“适合资源受限设备”；
- 对标准指标、常用库、常规训练设置作过长说明；
- 为避免审稿质疑而主动建议与核心贡献无关的统计显著性、置信区间、硬件测试或更多数据集。

更推荐直接写事实：

- 不写“为保证评价的公平性和可信性，我们严格……”，改为“模型配置由 Cell2 确定，随后冻结并在 Cell3–Cell8 上直接测试。”
- 不写“需要指出的是，尽管该指标不是最优，但这并不影响……”，改为“Transformer 在 Cell8 上误差最低；MS-AgentNet 的宏平均误差仍为五种模型中最低。”
- 不写“为了进一步证明所提模块的有效性和合理性……”，改为“通过移除 DSConv 和 RAA，比较各模块及其组合的贡献。”

## 9. 何时才建议补充分析或实验

只有满足下列至少一项时，才建议补充：

- 摘要、贡献或结论中的核心主张没有对应证据；
- 当前实验无法区分两个主要贡献的来源；
- 比较协议存在会改变结论的明显不公平；
- 数据使用方式可能造成真实泄漏；
- “轻量化”“在线”“部署”等核心措辞明显超出已有复杂度或硬件证据；
- 某个关键公式、实验设置或数据角色缺失，导致无法复现。

以下情况默认不建议补充：

- 仅为了让所有指标都显著；
- 仅为了回应假想审稿人；
- 仅为了让每个模块都有单独最优结果；
- 仅为了增加篇幅或分析数量；
- 现有主张已经由主对比、消融和复杂度结果充分支持。

## 10. AI 审查与改写流程

每次审查正文时按以下顺序执行：

1. **先读全文，不立即逐句修改。** 建立“问题—缺口—设计—证据—边界”的叙事图。
2. **复述本文价值。** 用不超过 150 字说明研究为什么值得做、亮点是什么。若无法清楚复述，先修主线。
3. **检查章节分工。** 判断每节是在推进主线，还是重复背景、结果或自我辩护。
4. **检查贡献闭环。** 每项贡献必须在方法和实验中都有对应内容。
5. **检查分析深度。** 核心机制、严格数据边界、代表性退化轨迹和复杂度需要深入；标准知识与旁支自证应简写。
6. **执行删冗测试。** 优先删去重复数字、重复结论、假想质疑和过度限定。
7. **再做语言提升。** 使用准确、克制、直接的学术表达，不用华丽但空泛的形容词。
8. **最后检查主张边界。** 不把域内泛化写成跨域泛化，不把部署潜力写成部署完成，不把相关性写成因果性。

## 11. AI 输出审查意见的格式

除非用户另有要求，审查结果按以下优先级输出：

1. 用一段话概括当前稿件的核心叙事及其是否清楚；
2. 列出最多 5 个真正影响价值表达或逻辑闭环的问题；
3. 指出应深入的内容与应一笔带过的内容；
4. 给出可直接删除或合并的冗余段落清单；
5. 最后才给出代表性改写示例。

不要输出几十条面面俱到的审稿清单。不要把“可以增加更多实验”当作通用建议。不要在没有阅读全文时进行局部措辞优化。

### 11.1 强制只审不改流程

除非用户明确说“可以修改”“按这个版本改”或给出等价授权，否则不得直接修改 `chapters/`、`tables/`、`figures/`、`main.tex`、`preamble.tex` 或其他论文正文与排版文件。默认工作方式是先提出修改方案，等待用户确认。

每一处建议修改必须按以下顺序展示：

1. **原文：** 完整保留待修改句子或段落，不得只摘取几个词而丢失语境；
2. **建议稿：** 给出可直接替换的完整版本；
3. **改后效果：** 说明读者将更清楚地看到什么，例如研究价值更早出现、贡献边界更准确、段落推进更集中或重复数字被压缩；
4. **修改原因：** 说明原文存在的具体问题，如价值出现过晚、信息顺序不符合领域叙事、句子承担过多功能、重复表格、机制归因过强或存在防御性限定；
5. **范文依据：** 回到 `学习/_bmsformer_text.txt` 或 `学习/工程应用AI(4).txt` 的对应部分，说明范文采用了什么叙事动作。依据应描述“范文怎么写、写到什么级别”，不能只写“参考顶刊风格”；
6. **处理建议：** 标明“建议替换”“建议删去”“建议合并”“建议保留”或“可选调整”。

推荐展示格式：

> **位置：摘要第 2 句**  
> **原文：** ……  
> **建议稿：** ……  
> **改后效果：** ……  
> **修改原因：** ……  
> **范文依据：** BMSFormer 摘要在提出资源约束后，立即指出现有方法依赖高开销结构，随后直接引出 BMSFormer；它不在摘要中展开合理性辩护。  
> **处理建议：** 建议替换，等待确认。

若建议删除，必须额外执行“删除后原意测试”：

- 删除后段落的核心判断是什么；
- 哪一句或哪一处表格仍然承载该信息；
- 为什么删除不会损害方法复现、证据完整性或结论边界。

不得使用“更学术”“更像顶刊”“表达更高级”等空泛理由。每个原因必须落到信息顺序、论证功能、细节颗粒度、证据强度或术语边界上。

### 11.2 范文回溯要求

提出改写前必须重新查看范文的对应部分，而不能仅凭此前总结或记忆生成理由：

- 修改摘要时，查看两篇范文的摘要及贡献浓缩方式；
- 修改引言时，查看问题提出、文献归并、挑战和贡献段落；
- 修改方法时，查看整体流程、模块数据流、核心公式与标准知识的篇幅分配；
- 修改结果时，查看范文如何选择代表性退化轨迹、如何报告数值以及如何连接结构设计；
- 修改结论时，查看问题、方法、证据、限制的收束顺序。

范文不是措辞模板，也不是正确性权威。只学习其叙事结构、信息颗粒度和证据组织；若范文本身存在防御性补证、过强归因或冗余，则应明确指出并拒绝继承。

### 11.3 分批确认规则

审查应按“摘要 → 引言 → 第 2 章 → 第 3 章 → 第 4 章 → 结论”的顺序分批进行。每批先给出：

- 本部分承担的论文功能；
- 当前版本是否完成该功能；
- 最多 5 项高优先级修改；
- 可直接删除或合并的冗余片段；
- 完整的改前/改后对照。

用户确认当前批次后，才可修改对应文件或进入下一批的大规模改写。若用户只要求继续审查，则继续提供对照方案，仍不得落盘修改正文。

## 12. 最终判断标准

一篇合格的修改稿应使领域读者形成以下清晰认识：

> 本文的价值在于同时改进了 SOH 估计的输入稳定性和轻量化序列建模。MS-CCCT 与双相关筛选为不同数据集构建稳定健康指标；MS-AgentNet 以多尺度 DSConv 和 RAA 协同建模局部变化与长期依赖，并控制参数和存储开销。四个数据集上的多电池结果支持域内跨电池表现，跨数据集实验则诚实揭示了目标域适应的收益与局限。

如果修改建议不能让这条认识更清楚、更可信或更简洁，就不应成为优先修改项。

## 13. 本文逐章内容清单与颗粒度边界

本节不是要求每一章机械串成同一种逻辑，而是规定每一部分应包含什么、写到什么程度、哪些内容不应进入。审查时必须逐项核对。

### 13.1 摘要：让读者在一段内看到整篇论文

**必须写：**

1. 一句说明准确、轻量 SOH 估计对资源受限 BMS 的价值；
2. 一句指出健康指标稳定性与模型计算开销两个缺口；
3. 一至两句交代输入端的自适应指标构建与模型端的 MS-AgentNet；
4. 一句说明四个异质公开数据集的验证范围；
5. 一个最有代表性的精度结果和一个模型规模结果；
6. 一句落到“精度—资源开销平衡”及应用潜力。

**写到的级别：** 方法名称、关键机制和量级结果。摘要不展开阈值、具体数据角色、全部指标组合、全部基线和所有提升率。

**不写：** 文献综述、公式、参数搜索、消融细节、迁移比例、限制条件辩护、多个近义结论。

**范文对应：** BMSFormer 摘要只保留 LGFA、多尺度 DSConv、三数据集与准确性/开销/稳定性；师兄稿摘要只提系统特征选择、SL-AgentNet、线性复杂度、代表性精度和硬件/存储结果。二者都把“做了什么”置于“为什么这个选择合理”之前。

### 13.2 引言开头：建立现实价值，不写成电池百科

**必须写：**

- 锂离子电池应用与退化带来的健康管理需求；
- 为什么容量型 SOH 有实际意义；
- 为什么 SOH 不能方便地直接在线获得；
- 为什么需要从运行信号中间接估计。

**写到的级别：** 2–3 个紧凑段落，使读者接受“SOH 估计值得研究”。

**一笔带过：** SEI、活性锂损失、电极结构衰退等机理只作为退化背景列举一次；碰撞、热失控等安全案例只保留能支撑应用价值的一处。

**不写：** 与本文输入特征和模型设计无关的安全事故长链条、储能领域宏观综述、材料体系发展史。

### 13.3 文献综述：不是按模型点名，而是把缺口推到本文面前

**必须写：**

- 模型驱动方法的物理解释性与在线参数/计算限制；
- 传统机器学习对人工特征质量的依赖；
- CNN 的局部优势与长程不足；
- RNN 的时序能力与串行计算限制；
- Transformer 的全局能力与二次开销、局部偏置不足；
- 混合模型与高效注意力的已有进展及仍存矛盾；
- 健康指标固定窗口、噪声敏感、跨电池波动与冗余问题；
- 数据域变化对泛化与迁移的影响。

**写到的级别：** 每类方法回答“它解决了什么—还缺什么—与本文哪项设计相关”。同一缺点只在最合适的位置讲透一次。

**一笔带过：** 每篇代表文献用一句说明其方法和与本研究相关的结论，不介绍完整网络结构或全部实验结果。

**不写：** 按年份罗列论文、对每个缩写作教材式解释、为了显示阅读量而加入与贡献无关的文献。

**范文对应：** BMSFormer 将文献压缩为“重型结构—精度/效率矛盾—稳定性”三类问题；师兄稿将文献压缩为“噪声敏感的特征—固定窗口—RNN/CNN/Transformer 的结构限制—嵌入式瓶颈”。需要学习的是这种问题归并，不是照搬其每一条文献。

### 13.4 挑战与贡献：把全文压缩成三个可检验承诺

**必须写：**

- 挑战 1：健康指标跨电池稳定性；
- 挑战 2：局部与长期信息的低成本协同建模；
- 挑战 3：严格泛化与复杂度评价；
- 与三项挑战逐一对应的贡献。

**写到的级别：** 每项贡献包含“区别于现有方法的设计动作 + 解决的具体问题 + 后文将提供的证据”。

**不写：** “首次”“填补空白”“全面优于”等无法稳健支撑的优先权语言；也不要把实验章节目录改写成贡献。

### 13.5 整体 SOH 估计框架：让读者先看到端到端流程

**必须写：** SOH 定义、数据获取、健康指标构建、滑窗样本、模型训练、配置确定和性能评价。

**写到的级别：** 一张流程图加四个阶段足够。重点是数据如何从原始循环记录变成模型输入及主对比结果。

**一笔带过：** SOH 定义的争议、监督学习和反向传播常识。

**范文对应：** 两篇范文都用一张流程图和四个步骤快速建立全局认知，随后才分别展开指标与模型。

### 13.6 数据集：只保留影响任务难度和泛化解释的信息

**必须写：** 来源、电芯数量、化学体系、标称容量、温度、主要充放电协议、本文使用的电芯，以及各数据集最关键的退化形态。

**写到的级别：** 一个汇总表加每个数据集一个短段。文字只补充表格无法表达、且后文会用于解释结果的特征，例如 Oxford 的动态放电历史、CX2 的阶段性转折、MIT 的多步快充。

**一笔带过：** 发布机构荣誉、公开数据集的一般价值、表格已有的数字。

**不写：** “权威、可靠、广泛认可、确保标准化”等自我评价性修饰；直接陈述来源和协议即可。

### 13.7 候选健康指标：定义清楚，但不要让候选池淹没主创新

**必须写：** 15 项候选指标来自哪些信号域；最终会由统一规则筛选；关键指标的定义、单位和提取边界。

**写到的级别：** 对 CCCT、局部放电容量、能量效率等与最终结果密切相关的指标给出公式和物理/运行含义。IC、DTV、DTC 等候选指标说明差分、平滑和提取的峰值/位置/宽度即可。

**一笔带过：** 未入选指标的全部峰形机理、标准微分分析方法的历史、每项特征的重复解释。

**不写：** 把每个候选指标都包装成本文贡献。本文贡献是自适应标定与筛选框架，不是发明所有候选特征。

### 13.8 MS-CCCT：这是输入端核心创新，应讲透搜索变量与组级稳健性

**必须写：**

- 固定窗口为何可能跨电池失效；
- 特征开发电池集合的组成；
- 候选窗口如何生成；
- PCC/SCC 分别衡量什么；
- 组级最小相关性得分为何代表共同适用性；
- 粗到细搜索如何停止；
- 指标规则如何在特征开发电池上确定，并固定应用于同组其他电池；
- 最终窗口在各数据集上的差异及其意义。

**写到的级别：** 给出必要公式、搜索阶段、阈值/步长和最终窗口，使方法可复现。解释重点放在“为什么用组内最低表现决定窗口”，而不是反复强调“这样没有泄漏”。

**一笔带过：** PCC/SCC 的统计教材内容、搜索流程中显然可由算法表看出的循环语句。

**范文对应：** BMSFormer 深入到多级窗口、停止条件和最终区间，但主要使用 PCC；本文的提升应明确落在“多节开发电池 + PCC/SCC 双相关 + 固定规则在同组其他电池上的应用”，而不是仅仅说搜索更细。

### 13.9 健康指标筛选：讲清准入与去冗余的不同角色

**必须写：** 双阈值准入、候选排序、双相关冗余判定、最终指标组合及其在同组其他电池上的稳定性。

**写到的级别：** 算法表承担完整步骤；正文解释两个阶段各解决什么，并用一个数据集示例说明跨电池波动和指标冗余确实存在。

**一笔带过：** 表 2-4、2-5 中每个数值；与文献指标比较只总结范围与相对位置。

**不写：** “结果证明阈值绝对正确”；阈值是开发阶段规则，只需说明设定与一致使用。

### 13.10 MS-AgentNet 总体架构：先讲数据流，再讲模块

**必须写：** 输入形状、嵌入、Block 顺序、SLFA、DSConv-L、FFN、读出层和残差连接。

**写到的级别：** 三个阶段公式足以固定数据流。读者应能回答“局部增强、全局交互、长尺度细化分别发生在哪里”。

**一笔带过：** 训练通过反向传播、LayerNorm 的常规作用、展平与线性回归常识。

**不写：** 在总体架构小节提前重复后续每个模块的全部细节。

### 13.11 多尺度 DSConv：重点是模块分工，不是卷积教科书

**必须写：** 深度卷积与逐点卷积如何降低密集卷积开销；DSConv-S 的短核、局部增强和局部路径作用；DSConv-L 的长核和后融合细化作用；两者为何位置不同。

**写到的级别：** 标准卷积与 DSConv 的参数/计算对比一次；两个模块分别给出结构公式和近似成本。对核长度 5 与 31 只陈述设计尺度与实验验证，不虚构唯一物理原因。

**一笔带过：** MobileNet、ShuffleNet 的背景；转置操作；残差连接的一般益处。

**范文对应：** BMSFormer 对标准卷积只写到能推出复杂度比值，随后马上进入短核/长核分工；师兄稿进一步用“局部突变—长期趋势”解释模块，但其中“aging inertia”等强物理措辞必须有证据才可使用。

### 13.12 RAA 与 SLFA：这是模型端核心创新，应讲到可验证、可复现

**必须写：**

- Softmax 注意力形成 $N\times N$ 交互及其二次复杂度；
- 线性注意力降低开销但不显式提供局部偏置；
- 静态智能体、通道缩放、ReLU² 行归一化；
- 智能体聚合与广播两阶段；
- 全零行的回退方式；
- 等效交互的秩上界与实际不构造完整矩阵；
- 全部注意力头的时间和存储复杂度；
- SLFA 中独立局部路径与 RAA 支路如何融合。

**写到的级别：** 核心公式、张量尺寸、计算顺序与复杂度必须完整。这里值得深入，因为它决定模型是否真有结构创新和线性复杂度。

**一笔带过：** 多头注意力的通用历史、Softmax 的基础定义、每个符号的重复解释。

**不写：** 未经实验支持的“ReLU² 必然更稳定/更稀疏/更符合物理规律”；只写其数学效果和本文观察到的结果。

### 13.13 评价指标：定义到可核查即可

**必须写：** MAE、MAPE、RMSE、R² 的公式和方向。

**写到的级别：** 每项一至两句。指出 MAPE 在真实值接近零时的限制即可。

**一笔带过：** “RMSE 对大误差更敏感”等常识不应展开成多段。

**不写：** 将标准指标包装成本文的“多维严谨评价体系”。

### 13.14 配置选择与训练稳定性：只为说明结果不是偶然调参

**必须写：** 搜索空间、配置选择电池、选定标准、最终冻结配置、独立重复次数；如保留收敛性分析，则说明阈值轮次与后期波动。

**写到的级别：** 一个表、一张热图和一个短讨论。重点是浅层小维度已经足够，且配置确定后不再针对评价电池调优。

**一笔带过：** 初始化分布的常规数学背景、每个超参数组合的逐项结果。

**可删或降级：** 如果不同初始化策略结论只是“差异很小”，且初始化不属于核心贡献，可压缩为一句或移至附录。师兄稿对此的长篇“dead neuron/可恢复范围”讨论属于修回颗粒度，不是本文默认写法。

### 13.15 域内 SOH 估计结果：选择代表性轨迹，不逐表解说

**必须写：**

- Oxford 宏平均结果；
- 至少一个局部偏离或快速下降案例；
- CX2 的阶段性转折/后段加速；
- CS2 与 MIT 的相对平滑退化；
- 模型并非每颗电池每项指标都最优；
- 与主要基线的代表性降低率。

**写到的级别：** 每类退化形态用一个段落：现象、模型差异、1–3 个关键数值、谨慎的结构联系。最后用宏平均收束。

**一笔带过：** 配置选择电池的曲线、所有电池的所有误差、同一降低率的 MAE/MAPE/RMSE 三遍重复。

**范文对应：** 师兄稿按“突降、容量恢复、加速衰减”组织结果讨论，这比按表格行顺序更有叙事性；但其把每种现象强归因到具体模块的写法应降一档，改为“与设计目标一致”，因果贡献留给消融。

### 13.16 跨数据集迁移：重点展示边界，而不是把负结果辩护成成功

**必须写：** 统一输入特征、源域与目标域、source-only、只微调读出层、目标域比例、直接迁移结果、适应后变化、源域依赖。

**写到的级别：** 先比较同体系 CALCE 数据域，再讨论涉及 Oxford 的域差异；同时报告误差和 R²。明确“误差下降”和“解释退化变化”是不同判断。

**一笔带过：** 每个方向所有数字；为什么负 R²“仍有意义”的防御性论证。

**正确语气：** 负 R² 就说明直接迁移或当前适配不足；它的研究价值在于揭示域偏移与目标域数据需求，而不是证明模型已经具有强跨域泛化。

### 13.17 消融实验：只回答贡献来源

**必须写：** 基础骨干、仅 DSConv、仅 RAA、完整模型；短核、长核和双尺度；四个数据集上的综合结果。

**写到的级别：** 先说明单模块收益具有数据集依赖，再说明完整组合更稳定。用 CX2 和 Oxford 的代表性结果展示组合收益；对 CS2/MIT 的小收益直接陈述。

**一笔带过：** 每个变体的每项指标、对单个微小差异的机制推演。

**不写：** 仅依据“组合增益大于单项增益”就宣称严格数学协同；如果没有专门交互设计与统计证据，使用“互补作用”比“超加性协同机制”更稳妥。

### 13.18 复杂度：轻量化主张要深入，但指标边界必须清楚

**必须写：** 统一输入和配置、FLOPs、参数量、存储占用、训练时间；不同模型宽度下的存储趋势。

**写到的级别：** 报告测量方式和统一设置，重点解释 MS-AgentNet 在参数量和存储上的优势，以及训练时间并非最快这一事实。

**一笔带过：** 常用 Python 库的功能说明、每个模型结构的重复介绍。

**不写：** 没有 MCU 或实时延迟实验时，不得写“已经满足实时部署”“可直接部署”。可写“具备资源受限 BMS 应用潜力”。

**范文对应：** BMSFormer 以 FLOPs、时间、参数和存储支撑“高效”；师兄稿因为明确主张硬件可行性，才进一步写延迟、吞吐、LUT 和 MCU。本文当前主张是轻量化潜力，不需要复制后一层证据。

### 13.19 结论：只收束，不再辩护

**必须写：** 输入端贡献、模型端贡献、四数据集主要发现、复杂度结果、跨域限制、未来方向。

**写到的级别：** 3 个短段：做了什么；获得什么证据；还受什么限制。

**一笔带过：** 全部提升率、全部模块细节、所有跨域方向。

**不写：** “虽然……但是这不影响……”式辩护；不重新证明阈值、初始化、模型深度或负迁移结果的合理性。

## 14. 从两篇范文提炼的领域叙事颗粒度

### 14.1 深入分析的判断标准

同时满足以下任意两项的内容应深入写：

- 是本文独有或实质改造的设计；
- 直接支撑标题、摘要或贡献中的核心主张；
- 决定实验是否公平、是否存在泄漏；
- 读者需要这些信息才能复现；
- 结果显示出与基线不同的代表性退化行为；
- 负结果揭示了方法的重要适用边界。

因此，本文应深入：MS-CCCT、双相关筛选、RAA/SLFA、训练与配置协议、主对比报告范围、代表性退化轨迹、消融、复杂度和跨域边界。

### 14.2 一笔带过的判断标准

满足以下任一项且不影响复现的内容应简写：

- 领域读者普遍掌握的标准知识；
- 表格或公式已经完整表达的信息；
- 与核心贡献没有直接对应关系的旁支背景；
- 只得出“差异不大”或“符合常识”的补充分析；
- 仅用于预防假想质疑的解释；
- 无法改变结论的更多数字、更多基线或更多统计包装。

因此，本文应简写：SOH/指标的教科书说明、公开数据集常识、标准模型原理、标准评价指标、库函数、无明显结论的初始化比较以及重复的百分比改善。

### 14.3 两篇范文共同采用的叙事动作

- 用应用约束界定研究问题，而不是从模型名称出发；
- 用现有方法的具体瓶颈引出模块，而不是先提出模块再寻找用途；
- 先给整体流程，再逐层展开；
- 核心模块写公式、尺寸、计算顺序和复杂度；
- 结果围绕退化形态与工程指标组织，不只围绕模型排名；
- 结论用少量代表性数字锚定贡献；
- 对限制采用直接陈述，不试图把限制重新包装成优势。

### 14.4 两篇范文中不应机械继承的内容

- BMSFormer 对标准卷积和标准注意力的部分解释偏教材化，可进一步压缩；
- BMSFormer 的“384 组超参数稳定性”服务于其明确提出的稳定性贡献，本文若不把它列为核心贡献，无需同等展开；
- 师兄稿中“rigorous、crucially、importantly、proves、validates”密度较高，容易形成自我评价；
- 师兄稿中初始化风险、统计显著性、置信区间、校准、延迟冲突解释和硬件 LUT 属于特定工程主张或修回补证；
- 将每种退化现象唯一归因于某个模块，会超出消融能够支持的因果颗粒度；
- “工程等效”“安全保守”“不会产生数据积压”等结论需要专门定义和硬件/统计证据，本文当前不应借用。

## 15. 顶刊常用表达的功能词库

词汇应承担逻辑功能，而不是制造“高级感”。同一段不要堆叠多个评价性副词。

### 15.1 描述研究缺口

- remains limited by：仍受到……限制
- is sensitive to：对……敏感
- relies on empirically fixed windows：依赖经验固定窗口
- incurs quadratic computational overhead：产生二次计算开销
- lacks explicit local inductive bias：缺少显式局部归纳偏置
- remains challenging under domain shifts：在数据域变化下仍具挑战
- has not been systematically evaluated：尚未得到系统评价

### 15.2 描述方法动作

- formulate ... as a search variable：将……表述为搜索变量
- calibrate on feature-development cells：在特征开发电池上标定
- impose a group-wise robustness criterion：施加组级稳健性准则
- screen out redundant indicators：筛除冗余指标
- mediate global information exchange：介导全局信息交互
- preserve fine-grained local information：保留细粒度局部信息
- refine long-horizon degradation trends：细化长尺度退化趋势
- freeze the selected configuration：冻结所选配置
- evaluate directly on unseen cells：直接在未见电池上评价

### 15.3 描述证据

- results show/indicate that：结果显示/表明
- maintains lower errors under：在……条件下保持较低误差
- yields the best aggregate performance：取得最佳综合表现
- remains competitive across：在……范围内保持竞争力
- exhibits dataset-dependent gains：表现出数据集依赖的收益
- improves after target-domain adaptation：经目标域适应后改善
- reveals a clear source-domain dependence：揭示明显的源域依赖
- supports the intended role of：支持……的预期作用
- is consistent with the design objective of：与……的设计目标一致

### 15.4 描述边界

- does not necessarily imply：并不必然意味着
- remains insufficient to：仍不足以……
- is restricted to：受限于……
- depends on the availability of：取决于……的可获得性
- should be interpreted together with：应结合……理解
- indicates potential rather than completed deployment：体现应用潜力，而非已经完成部署

### 15.5 控制表述强度

推荐从弱到强按证据选择：

> suggests → indicates → shows → supports → demonstrates

通常避免：

> proves、guarantees、fully resolves、universally applicable、unambiguously confirms

只有数学定理、严格定义下的验证或直接硬件证据才允许使用接近“证明/确保”的强词。

### 15.6 常见防御性句式的清理

- “值得注意的是/需要指出的是”若删除不改变事实，直接删除；
- “为了进一步验证……的有效性和合理性”改为实验动作本身；
- “尽管……但这并不影响……”改为并列报告局部结果与总体结果；
- “这充分证明了……”改为“结果表明/支持……”；
- “为确保公平性、严谨性和可信性”改为具体数据角色与冻结协议；
- “可能是由于某模块……”若没有消融支持，改为“与该模块的设计目标一致”或删除；
- “从而验证了模型可部署性”在无硬件验证时改为“表明较低的存储开销/部署潜力”。

## 16. 如何让审稿人看见研究价值与亮点

### 16.1 两篇范文采用的价值传达方式

两篇范文都没有只把模块结构摆在审稿人面前，而是先建立一个明确的工程矛盾，再让每项设计成为对该矛盾的直接回应。

**BMSFormer 的价值传达链条：**

> 资源受限 BMS 需要准确的在线 SOH 估计 → 现有高性能深度模型往往依赖重型结构，难以兼顾精度、效率与稳定性 → 从高频 SOC 片段中筛选强相关单一健康指标，减少输入冗余 → 用 LGFA 同时建模局部与长期信息，并用多尺度 DSConv 补充特征多样性 → 用跨电池精度、复杂度和超参数稳定性完成实验闭环。

其亮点不是孤立的“提出 LGFA”或“使用 DSConv”，而是这些设计共同服务于“准确、紧凑且稳定的在线 SOH 估计”这一中心价值。审稿人在摘要、引言问题归纳、贡献条目、方法开头、结果分类和结论中都会再次遇到同一个中心命题。

**师兄 SL-AgentNet 的价值传达链条：**

> 资源受限车载 BMS 面临特征在线提取负担和 Transformer 二次开销 → 用系统特征筛选寻找信息密度更高的局部电压区间 → 用单智能体线性交互降低全局建模成本 → 用局部卷积和多分支结构补充被压缩的细节与非线性 → 用突降、容量恢复、加速衰减等代表性现象解释模型为何有用 → 用模型规模、延迟和 MCU 验证把工程价值落地。

其有效之处是把“模型模块”翻译成“能处理哪类真实退化困难”。需要谨慎的是，修回稿后半部分对每个模块、硬件指标和统计现象都进行了强解释；本文应学习其价值映射方式，不复制其全部防御性证据。

### 16.2 本文应让审稿人记住的中心命题

审稿人读完后应能够用一句话复述本文：

> 本文不是单纯追求更低预测误差，而是通过健康指标与预测模型的协同设计，同时提高输入的跨电池稳定性并降低局部—全局序列建模的资源开销。

进一步应记住三个亮点：

1. **健康指标不是固定经验窗口。** 本文将 CCCT 观测区间作为可标定变量，在多节特征开发电池上以组级 PCC/SCC 约束搜索，并通过双相关准入与冗余剔除确定输入；规则确定后固定应用于同组其他电池。
2. **模型不是重型模块的直接堆叠。** DSConv-S、RAA 和 DSConv-L 按局部增强—全局交互—长尺度细化的顺序协同工作；RAA 通过少量智能体将相关性交互由二次形式降低为关于序列长度的线性形式。
3. **实验不是只展示单一数据集的最优精度。** 四个数据集的多电池结果支撑域内跨电池表现，消融与复杂度分析解释性能来源和资源代价，跨数据集实验进一步揭示少样本适应的收益及源域依赖。

任何摘要、引言、贡献、结果或结论的修改，都不得模糊这三个记忆点。

### 16.3 不同位置如何递交研究价值

| 位置 | 审稿人此时需要知道什么 | 推荐写法 |
| --- | --- | --- |
| 标题/摘要开头 | 研究解决的不是一般回归问题，而是资源受限 BMS 中的可靠 SOH 估计 | 直接提出输入稳定性与计算效率的双重约束 |
| 摘要方法部分 | 本文从哪两个层面回应问题 | 先写自适应健康指标，再写轻量化局部—全局建模 |
| 引言前半 | 为什么问题值得研究 | 从在线不可直接测量、跨电池变化和资源限制建立现实需求 |
| 文献综述结尾 | 现有工作还缺哪一块 | 收束为输入端稳定性、模型端协同效率和评价端泛化证据 |
| 贡献条目 | 本文做了哪些可检验的新动作 | 每项包含设计动作、区别和对应证据，不写空泛意义 |
| 方法章开头 | 这个模块为何存在 | 先指出它解决的具体缺口，再给数据流与公式 |
| 结果章 | 设计带来了什么实际差异 | 围绕退化形态、综合误差、消融和资源开销组织证据 |
| 结论 | 审稿人应带走什么 | 重述双层协同设计、代表性证据和适用边界 |

研究价值不应只在引言和结论各出现一次。它应在不同章节以不同形式出现：引言给出问题价值，方法给出设计回应，结果给出证据，结论完成收束。这里的“重复”是贡献闭环，而不是原句复述。

### 16.4 把技术模块翻译成审稿人能判断的亮点

不要只写：

- “本文提出 MS-CCCT。”
- “本文采用 DSConv-S、RAA 和 DSConv-L。”
- “本文在四个数据集上进行了实验。”

应写成：

- “MS-CCCT 将经验设定的充电电压窗口转化为组级标定问题，使同一特征规则能够由多节开发电池共同约束，而不是被单节电池的偶然高相关性决定。”
- “RAA 以少量智能体介导跨位置交互，避免形成完整的 $N\times N$ 相关性矩阵；独立局部路径与多尺度 DSConv 则补充被压缩的细粒度和长尺度退化信息。”
- “四数据集实验分别回答三个问题：方法能否推广至未见电池、性能改善来自哪些模块、轻量化优势是否以精度损失为代价。”

亮点表达应包含“改变了什么”和“为什么对任务重要”，不能只列模块名称。

### 16.5 用实验回答价值主张，而不是堆积实验数量

每类实验只承担一个主要论证任务：

- 健康指标相关性：回答输入规则能否在未见电池上保持稳定；
- 域内主对比：回答模型能否在未见电池上获得有竞争力的综合精度；
- 代表性曲线：回答模型面对局部波动、阶段性转折和长期平滑退化时表现如何；
- 消融：回答精度改善是否来自 DSConv 与 RAA 的互补组合；
- 复杂度：回答精度是否依赖更大的模型规模；
- 跨数据集迁移：回答域变化下直接迁移的局限，以及少量目标域信息能够改善到什么程度。

如果新增分析不能回答新的核心问题，就不应仅作为“更全面”的装饰加入正文。

### 16.6 面向审稿人的价值表达模板

**引言缺口收束：**

> 现有研究分别改善了健康指标提取、局部退化建模或长程信息交互，但输入特征的跨电池稳定性与模型端的轻量化局部—全局协同仍缺少统一设计。由此，即使模型在单一数据域中取得较低误差，其性能是否能够延续至未见电池，以及这种性能需要付出怎样的资源代价，仍有待系统评价。

**方法价值总述：**

> 为解决上述问题，本文从输入表征与序列建模两个层面展开设计。输入端通过组级自适应窗口标定和双相关筛选获得稳定、低冗余的健康指标；模型端通过多尺度深度可分离卷积与智能体介导的线性交互，在较低参数开销下融合局部变化和长期退化信息。

**结果价值收束：**

> 综合主对比、消融与复杂度结果可以看到，MS-AgentNet 的性能改善并非来自模型规模扩张，而是来自健康指标稳定性与局部—全局特征交互的协同设计。跨数据集结果同时表明，这种域内优势不会自动转化为无条件的跨域泛化，少样本适应效果仍受到源域选择和目标域数据量影响。

### 16.7 防止价值表达滑向防御性写作

价值表达回答“为什么值得做、本文改变了什么、证据说明了什么”；防御性表达则反复回答“为什么作者没有错”。二者按以下方式区分：

- 价值表达：“健康指标规则确定后固定用于同组其他电池，因此结果反映该规则的跨电池稳定性。”
- 防御性表达：“为了避免审稿人质疑数据泄漏，本文严格确保评价电池绝对没有以任何形式参与……”
- 价值表达：“Transformer 在 Cell8 上误差最低，而 MS-AgentNet 在六颗电池的宏平均结果中最优。”
- 防御性表达：“虽然 Cell8 不是最优，但这并不意味着本文方法无效，因为……”
- 价值表达：“模型仅调整读出层后误差下降，但部分方向的 R² 仍为负，说明跨域表示尚不充分。”
- 防御性表达：“尽管 R² 为负，考虑到跨域任务非常困难，该结果仍然可以接受……”

审查时保留前一类，删除或重写后一类。

## 17. 两篇范文逐章节价值传达剖析

本节首先解释范文本身如何向审稿人递交研究价值。学习时不要急于把范文章节替换成本文内容，而要识别每一部分在审稿人认知中完成了什么任务。

### 17.1 BMSFormer：逐章节向审稿人传达了什么

#### 摘要：先建立“效率与精度必须同时解决”的中心价值

摘要首先告诉审稿人，研究场景是资源受限设备中的 BMS，而不是一般离线预测。随后立即指出现有先进 SOH 方法依赖资源消耗型结构，再提出 BMSFormer。方法介绍只保留两项最关键设计：LGFA 同时获取长期与短期依赖，多尺度 DSConv 以较少参数增强特征多样性。最后用三个异质数据集以及准确性、计算消耗和超参数稳定性收束。

这一部分向审稿人传达的是：

> 研究价值不只是“误差更低”，而是让高性能深度 SOH 估计更接近资源受限 BMS 的实际条件。

它没有在摘要中解释每个卷积核、窗口搜索步骤或训练协议，因为这些不是审稿人第一次接触论文时需要记住的信息。

#### 引言第一层：为什么在线 SOH 估计值得做

文章从锂离子电池的广泛应用、安全风险和 BMS 功能切入，紧接着指出 SOH 无法直接测量，而嵌入式设备又受存储和计算条件限制。

这一层不是为了完整介绍电池，而是让审稿人接受两个前提：

1. SOH 估计具有安全与运行价值；
2. 解决方案必须考虑在线和资源条件。

因此，后续所有“高效、紧凑、在线”的设计都有了应用依据。

#### 引言第二层：为什么现有方法没有解决好

文章先区分模型驱动与数据驱动方法，再沿 CNN、RNN、Transformer 展开。每类方法只保留与中心矛盾相关的能力和限制：CNN 擅长局部特征但长程建模不足；RNN 能传递历史信息但串行依赖限制效率；Transformer 能全局建模但标准注意力具有二次复杂度；已有混合模型往往继续叠加模块，使结构更复杂。

这一层向审稿人传达的是：

> 现有模型不是“不准确”，而是在提高精度时往往付出结构和计算代价；本文的问题是如何改变这种取舍。

#### 引言第三层：把文献缺口压缩成三个审稿问题

BMSFormer 将问题明确归纳为计算复杂度限制、估计精度挑战和性能稳定性问题。这个动作很关键：它把前面的文献从“相关工作列表”变成审稿人可以检查的三个问题。

审稿人随后会自然期待：

- 方法章节说明如何降低复杂度并保持表达能力；
- 实验章节同时报告精度与资源开销；
- 超参数实验说明稳定性是否成立。

#### 贡献条目：告诉审稿人本文具体改变了什么

三项贡献依次为：从高频 SOC 片段中逐步搜索强相关 HI；构建 LGFA 捕获短期与长期特征并降低复杂度；嵌入大小核 DSConv 融合多尺度与多通道信息。

这一部分的价值在于把抽象问题变成可核查的设计动作。它没有把“使用三个数据集”“比较四个模型”单独包装成主要创新，而是让实验服务于前三项设计。

#### 第 2 章整体流程：告诉审稿人研究是怎样完整运行的

整体流程用数据采集、特征工程、模型训练和模型评价四步连接原始数据与最终结论。这里按第四章正文交代训练电池、配置电池、主对比报告范围和超参数组合，不新增第三类电池角色。

这一章向审稿人传达的是：

> 这不是一个孤立网络模块，而是一套从在线可获得片段、健康指标、预测模型到泛化评价的完整 SOH 估计流程。

细节颗粒度停留在“阶段和数据角色”，没有在此处提前推导所有模块。

#### 数据集部分：告诉审稿人证据覆盖了哪些差异

Oxford、NASA 和 CALCE 分别提供不同电芯形式、化学体系、容量、协议和退化形态。文章通过表格交代完整配置，文字重点指出不同类型电池之间的差异。

这一部分不是为了证明数据集“权威”，而是为后续泛化主张限定范围：方法至少在这些材料和工况差异下接受了检验。

#### 健康指标部分：告诉审稿人输入为什么适合在线估计

BMSFormer 不从所有原始曲线直接学习，而是在高频中间 SOC 片段内逐级缩小窗口，以跨电池最小 PCC 选择强相关 CCCT/CCDT，并与文献健康指标比较。

这一部分传达两层价值：

- 窄区间时间特征降低完整曲线依赖，适合在线提取；
- 逐级搜索比固定经验窗口更能找到高相关区域。

它写到窗口、步长、停止条件和最终区间，因为这些决定方法能否复现；PCC 的一般统计原理则只需简述。

#### 模型总体架构：告诉审稿人各设计怎样协同工作

模型章先给出输入、嵌入、LGFA、DSConv-L、MLP 和输出的数据流，再逐一展开。这样审稿人在进入公式前已经知道每个模块的位置。

这一部分传达的是：

> BMSFormer 不是把两个卷积和一个注意力随意放在一起，而是用注意力处理局部—全局交互，再用长核卷积细化融合表示。

#### 多尺度 DSConv：告诉审稿人“轻量化”不是删掉特征能力

文章先比较标准卷积与深度可分离卷积的成本，再区分 DSConv-S 和 DSConv-L。短核面向局部变化和注意力输入增强，长核面向较长时间尺度特征。

这一节的价值传达方式是“成本公式 + 功能分工”：审稿人不仅看到参数更少，也看到节省计算后由什么结构保留多尺度表达。

#### LGFA：告诉审稿人全局能力和线性复杂度如何同时获得

文章先回顾 Softmax 注意力和线性注意力的关键差异，再提出 LGFA。标准理论只写到足以建立二次复杂度与表达不足这两个缺口，随后把主要篇幅交给本文模块。

这一节向审稿人传达的是：

> 本文并非简单把 Softmax 换成 ReLU，而是用局部卷积补偿线性注意力的表达限制，从而形成“低复杂度 + 局部敏感性 + 全局感受野”的组合。

#### Oxford 精度结果：先在一个细节丰富的数据集上展示模型行为

文章不只报告平均误差，还观察局部突降、误差分布和不同电池上的跟踪差异。它用 Cell2、Cell6 等代表性变化说明模型对局部退化的响应，再用八颗电池的平均结果收束。

这一部分向审稿人传达的是：

> 模型优势不仅存在于表格均值，也体现在对真实退化轨迹局部变化的跟踪上。

#### 效率结果：把标题中的“efficient”落实为多种资源证据

文章报告 FLOPs、训练时间、参数量和存储大小，并在统一配置及多个模型宽度/层数组合下比较。它也直接承认部分指标并非全部最优，再从整体精度—资源平衡评价模型。

这一部分让审稿人能够判断：轻量化是否只是参数量较小、是否依赖单一配置、是否以明显精度损失为代价。

#### 384 组超参数稳定性：专门兑现引言中的第三个问题

因为引言明确把性能稳定性列为研究问题，文章才用大量超参数组合比较 R² 分布和高性能配置比例。

这一部分传达的是：

> 模型的结果不只来自一个精心挑选的最优配置，它在更宽的配置空间中仍具有较稳定的表现。

这是一项与其贡献高度绑定的证据，而不是所有 SOH 论文都必须具备的常规实验。

#### NASA/CALCE 泛化结果：把单数据集优势扩展到不同退化模式

文章使用固定模型在同数据集其他电池上直接测试，并讨论容量恢复和非线性后段衰减。

这一部分向审稿人传达的是：

> BMSFormer 的有效性不是 Oxford 特定轨迹上的偶然结果，在其他化学体系和退化形态中仍保持综合优势。

这里支撑的是跨电池和跨数据类型适用性，而不是严格意义上的跨数据集零样本迁移。

#### 结论：让审稿人带走“平衡”而不是模块清单

结论再次回到精度与效率难以兼顾的问题，随后用 LGFA、多尺度 DSConv、三数据集结果和复杂度/稳定性证据收束，最后提出结构优化、更多电池类型和实际 BMS 部署。

审稿人最终被引导记住的是：BMSFormer 提供了一种在准确性、效率、稳定性和泛化之间取得较好平衡的 SOH 估计方案。

### 17.2 师兄 SL-AgentNet：逐章节向审稿人传达了什么

#### 摘要：把研究价值明确落到嵌入式部署

摘要从资源受限 BMS 中深度学习的计算瓶颈切入，先提出系统特征筛选，再提出线性复杂度 SL-AgentNet，最后给出 Oxford 精度改善、40.92 KB 模型规模和 Cortex-M4 验证。

它向审稿人传达的是：

> 这项工作的终点不是离线误差表，而是能在嵌入式条件下运行的 SOH 估计框架。

因为它明确主张嵌入式可行性，所以摘要才有资格使用 MCU 结果；没有硬件证据的论文不能照搬这一落点。

#### 引言：先建立“特征负担 + 模型开销”的双重瓶颈

文章将直接容量测量的在线困难、微分特征的噪声与预处理负担、RNN/CNN 的建模局限和 Transformer 的二次开销串联起来。

这一部分向审稿人传达的是：

> 资源问题既存在于输入特征提取，也存在于预测模型，必须采用双层解决方案，而不是只压缩网络。

这也是本文可以重点借鉴的价值结构。

#### 相关工作：把本文定位成三重矛盾的解决方案

相关工作分别讨论模型驱动、健康指标和深度架构，最终收束为噪声敏感、计算负担和特征表达能力之间的三难问题。

它不是为了证明所有前人都不足，而是为后续三个设计方向建立位置：系统特征筛选、线性智能体注意力、特征多样性补偿。

#### 整体方法与数据：说明研究不仅有模型，还考虑早期预测和异质退化

流程图交代数据、系统特征工程、模型开发和性能评价；数据集文字刻意指出 Oxford 的动态放电历史、NASA 的容量恢复和 CALCE 的后段“diving”行为。

这一部分提前告诉审稿人后续为什么要分析突降、恢复和非线性尾部。数据描述因此成为结果叙事的伏笔，而不是资料堆积。

#### 健康指标理论优势：先证明为什么选择这一类输入

文章比较能量积分与 CCCA/CCDA，强调电流噪声、热漂移和乘性耦合可能积累误差，再提出只依赖电压积分的伪能量指标。

这一节向审稿人传达的是：

> 输入设计不仅相关性高，而且试图减少电流测量与在线预处理负担。

它写得较深，是因为“降低在线特征提取负担”属于文章核心工程主张。本文若没有相同传感器噪声实验，不应照搬其强噪声鲁棒性结论。

#### 多尺度窗口筛选：把固定经验特征改写成离线标定问题

文章用粗细窗口、PCC 校准、区间冻结和部署三个步骤说明输入规则如何确定。不同数据集得到不同区间，以此强调退化敏感区域具有化学体系依赖性。

这一节向审稿人传达的是：

> 特征窗口不是人工拍定，而是历史数据驱动、部署前冻结的可配置规则。

#### Novelty and applicability 小节：直接替审稿人总结输入端亮点

这一小节把方法重新概括为“从静态窗口到数据驱动超参数搜索”，并说明离线确定后在线只需处理窄区间。

它的作用不是增加新方法，而是在健康指标章节结束时明确告诉审稿人：这一节的创新和工程意义分别是什么。

这种段末价值收束值得学习，但一句即可；重复表格和在线优势会变成冗余。

#### 模型总体架构：用级联数据流强调模块不是简单并联堆叠

文章明确把流程分成全局聚合、局部细化和非线性映射，并用公式固定残差来源与顺序。

它向审稿人传达的是：

> 轻量化网络仍然具有分工明确的多阶段表示能力，模型精度不是通过无序增加分支获得。

#### S/L-DSConv：把小核与大核分别映射到真实退化信息

S-DSConv 被解释为局部增强和低秩交互的旁路补偿；L-DSConv 被解释为融合后的长期趋势细化。

它向审稿人传达的是：两个核不是“多尺度”标签下的重复模块，而是在不同位置解决不同信息损失。

#### AFF、AAT-1d、FLFA、FDFA、PIAF：说明轻量化之后如何补偿表达能力

这一组小节围绕一个问题展开：单智能体虽然带来 $O(Nd)$ 复杂度，但可能压缩表示，因此需要局部满秩旁路、幅值显著性分支和异质激活补充。

向审稿人递交的核心价值是：

> 文章不是只降低注意力复杂度，而是同时考虑降低复杂度后可能损失什么，并通过结构设计补回来。

这是师兄稿模型叙事最值得学习的地方。需要避免的是把每个补偿模块都写成具有确定电化学物理意义；其主要证据仍然来自消融与数据现象的一致性。

#### 训练稳健性与超参数：回应单智能体和浅层结构是否可靠

文章详细讨论截断正态初始化、收敛速度、后期波动、历史参考电池和浅层配置。它试图回答审稿人可能提出的两个疑问：静态单智能体能否稳定训练；$L=1,d=16$ 是否因为模型能力不足。

这一部分属于“核心结构风险的针对性补证”。可学习其问题意识，但不应把同等篇幅当作默认要求。若本文 RAA 初始化不是核心争议，简写配置和收敛结果即可。

#### Oxford 结果：用突降与线性衰减展示局部—全局分工

文章将 Cell2/6 归为瞬时下降，将 Cell1/3/7/8 归为平滑衰减，分别讨论响应滞后和稳态误差，再把 S-DSConv 与 L-DSConv 的设计目标映射到这些现象。

向审稿人传达的是：

> 局部—全局结构不是抽象设计，它对应电池退化曲线中两类可观察困难。

这种按现象组织结果的方式应学习；“某模块导致某现象改善”的因果强度应由消融控制。

#### NASA/CALCE 结果：用容量恢复与加速衰减扩展价值范围

NASA 用容量恢复峰说明局部显著变化，CALCE 用后段快速下潜说明全局非线性趋势。文章据此把模型价值从 Oxford 的局部突降扩展到更复杂的退化形态。

向审稿人传达的是：模型的局部与全局能力在不同数据集上各有可见作用，而不是只在平均误差上占优。

#### 宏观消融：区分“特征好”还是“模型好”

文章首先比较相同模型下的新旧健康指标，再比较相同健康指标下 Transformer 与 SL-AgentNet。

这是一个非常明确的审稿问题：

> 性能改善到底来自高相关输入，还是来自网络结构？

宏观消融通过控制其中一端回答贡献归属，使“输入端 + 模型端”的双层价值都获得独立证据。

#### 微观消融：给模块分工提供实验支撑

文章分别替换大小核卷积、比较 PIAF 与标准 MLP、组合 FDFA 与 FLFA。这里不再介绍模块，而是检查前文提出的功能分工是否能在不同数据集上带来一致或互补收益。

向审稿人传达的是：完整模型的收益不是来自未经检验的结构堆叠。

#### 复杂度、延迟和硬件：兑现“可部署”这一强主张

因为题目和摘要都将研究定位为资源受限系统，文章不仅报告参数、存储和 FLOPs，还讨论 batch=1 延迟、吞吐、RTOS 调度、LUT 以及 Cortex-M4 结果。

这一部分向审稿人传达的是：工程价值已经从理论复杂度推进到实际执行层。

但其中“宏观 FLOPs 与微观延迟不矛盾”“1% 误差阈值”“不会积压”等长篇说明，具有明显审稿回复性质。只有当正文保留对应强主张时才需要。

#### 统计显著性与不确定性：修回阶段的可靠性补证层

配对检验、Bonferroni 校正、效应量、保形预测、覆盖概率和校准图回答的是“平均误差优势是否稳定”和“预测区间是否可信”。

它向审稿人传达的是：模型不仅给点预测，还尝试量化不确定性和工程风险。

但这已经形成第二条研究线。如果论文标题、摘要和核心贡献不包含不确定性估计，就不应默认加入；否则会稀释健康指标与轻量网络的主叙事。

#### 结论：把最终价值落到实际 BMS 约束

结论依次回收系统特征选择、线性复杂度模型、多数据集精度、40.92 KB 存储和 MCU 结果，最后承认依赖稳定恒流片段，并提出更多化学体系和动态工况。

审稿人最终被引导记住的是：SL-AgentNet 通过输入筛选、线性模型和硬件实现共同推进了资源受限 BMS 中的实际可用性。

### 17.3 两篇范文如何持续强化中心命题

它们并不是简单重复同一句“模型高效”，而是在不同章节递交不同层次的信息：

| 章节位置 | 递交给审稿人的信息 |
| --- | --- |
| 摘要 | 中心价值及最强证据 |
| 引言背景 | 为什么这个问题值得解决 |
| 文献综述 | 现有方法为什么还不够 |
| 挑战/贡献 | 本文承诺解决哪些具体问题 |
| 整体流程 | 研究怎样从数据走到结论 |
| 健康指标 | 输入端改变了什么 |
| 模型方法 | 模型端改变了什么 |
| 主结果 | 改变后是否确实更有效 |
| 代表性曲线 | 优势体现在哪类退化困难中 |
| 消融 | 优势来自哪里 |
| 复杂度/硬件 | 工程价值是否有资源证据 |
| 泛化/迁移 | 价值能延伸到什么范围 |
| 局限 | 哪些场景尚不能覆盖 |
| 结论 | 审稿人最终应记住什么 |

这才是可以学习的“重复强化”：每章回答不同问题，但所有回答都指向同一个中心价值。

### 17.4 从范文剖析回到本文时的使用规则

先完成范文剖析，再检查本文是否有对应的价值递交，不得反过来为了模仿范文章节而增加内容。

- BMSFormer 有稳定性贡献，所以有 384 组超参数实验；本文只有在保留同等主张时才需要相应篇幅。
- 师兄稿有嵌入式可行性贡献，所以有延迟、LUT 和 MCU；本文当前只主张较低存储与部署潜力，不需要复制硬件章节。
- 师兄稿有不确定性补证，所以有第 6 节；本文若不研究区间预测，不应加入同类分析。
- 本文新增的真正价值是更严格的组级双相关健康指标构建、RAA 设计、训练与配置边界、多电池结果和跨数据集适应边界；这些才应获得相对范文更深入的篇幅。

## 18. 引言修改专项规则：以研究价值而非防御性自证组织叙事

本节根据 BMSFormer（Energy, 2024）与 SL-AgentNet（Engineering Applications of Artificial Intelligence, 2026）的引言重新提炼，专门约束当前第 1 章修改。执行引言审查与改写时，本节优先于一般性的语言润色偏好；若与正文修改授权规则冲突，仍以“先审后改、用户确认后落盘”为准。

### 18.1 引言的根本任务

引言不是用来证明“作者这样做没有错”，而是依次让审稿人形成三个判断：

1. 本文处理的问题具有明确的科学或工程价值；
2. 现有方法在这个具体问题上仍留下可定位的缺口；
3. 本文设计是对这些缺口的自然回应，并且后文有相应证据可以检验。

引言的核心认知链必须是：

> 应用需求与研究价值 → 现有方法的具体能力 → 目标场景下仍存在的缺口 → 本文设计动作 → 预期价值与证据安排。

不得把认知链写成：

> 作者选择了某方法 → 解释为什么该选择没错 → 预防可能的质疑 → 增加更多分析自证 → 再回头说明研究意义。

任何句子如果主要用于回答“为什么作者没做错”，而不能帮助审稿人理解“为什么值得做、改变了什么、证据说明什么”，应优先删除、压缩或改写为直接事实。

### 18.2 两篇范文的可学习叙事动作

#### 18.2.1 BMSFormer：用资源约束统一评价现有方法

BMSFormer 的引言先建立“SOH 必须估计，但 BMS 计算与存储资源有限”的应用矛盾，再用同一尺度评价模型驱动方法、传统机器学习、CNN、RNN、Transformer 和混合模型。其有效动作包括：

1. 先限定资源受限 BMS 场景，使“高效、紧凑”具有应用依据；
2. 每类方法只保留与精度、长程建模和资源开销有关的能力与限制；
3. 将文献压缩为计算复杂度、估计精度和性能稳定性三个审稿问题；
4. 贡献条目写“识别了什么、构建了什么、融合了什么”，而不是罗列实验目录；
5. 后文分别用精度、复杂度和超参数实验兑现引言中的承诺。

不应机械继承的内容：

- CNN、RNN、Transformer 的部分教材式说明；
- 只有在稳定性被列为核心贡献时才合理的 384 组超参数分析；
- 将“结果稳定”扩展为独立研究线后产生的大量配置自证。

#### 18.2.2 SL-AgentNet：建立输入端与模型端的双重瓶颈

SL-AgentNet 的引言把直接容量测量、健康指标提取负担、固定窗口、微分噪声与 Transformer 二次开销放入同一工程场景。其有效动作包括：

1. 指出资源问题同时存在于输入特征提取与预测模型；
2. 将 IC/DV 噪声敏感、原始曲线冗余和固定窗口适应性不足归并为输入端缺口；
3. 将 CNN 局部能力、RNN 串行限制、Transformer 二次开销和高效注意力的表达压缩归并为模型端缺口；
4. 用“系统特征筛选 + 线性全局交互 + 局部细节补偿”回应双重瓶颈；
5. 用突降、容量恢复和加速衰减等退化现象把模块作用翻译为可观察价值。

不应机械继承的内容：

- 与 SOH 主线联系较弱的宏观能源、材料和电网背景；
- 在引言中过早强化 rank collapse、硬件 readiness 等高证据负担主张；
- 初始化、统计显著性、不确定性、校准、延迟冲突解释和 LUT/MCU 等修回补证层；
- 将每种退化现象唯一归因于某个模块的过强因果表述。

### 18.3 本文引言必须建立的审稿人认知递进

#### 第一级：问题为什么值得解决

审稿人应看到：容量型 SOH 与运行安全、寿命管理和使用决策有关，但最大可用容量难以在正常运行中直接获得，因此需要从可观测运行信号间接估计。

写到的级别：应用价值、SOH 的容量含义、直接测量困难和间接估计需求。

一笔带过：SEI 增厚、活性锂损失、电极衰退、短路、热失控和机械滥用只用于建立背景，不形成独立综述线。

#### 第二级：间接估计为什么仍然困难

在引言前部尽早提出两项相互关联的约束：

- 输入端：健康指标需要在不同电池、协议和退化轨迹下保持稳定，并避免噪声敏感和信息冗余；
- 模型端：预测模型需要在有限资源下同时表征局部退化变化和长期演化趋势。

这两项约束是全文导航，不能等到文献综述结尾才首次合流。

#### 第三级：现有方法具体留下了什么缺口

文献不按模型名称和发表年份罗列，而按以下问题归并：

1. 模型驱动与传统机器学习为什么难以满足本文目标场景；
2. CNN、RNN、Transformer 分别提供什么能力，又分别缺少什么；
3. 混合模型为何可能以更高结构开销换取表达能力；
4. 高效注意力降低全局成本后为什么仍需局部补偿；
5. 固定窗口和单一相关性筛选为什么难以得到稳定、互补的输入；
6. 域变化为什么要求区分域内跨电池评价与跨数据集迁移。

每类文献采用“已有能力 → 与本文相关的限制 → 导向哪项设计”的三步表达。同一缺点只在最合适的位置讲透一次。

#### 第四级：本文设计改变了什么

本文缺口收束必须明确表达：现有研究通常分别改善健康指标构建或序列模型，但输入的跨电池稳定性与轻量局部—全局建模仍缺少协同设计。

随后分别引出：

- MS-CCCT：把经验固定窗口转化为由多节特征开发电池共同约束的组级标定问题；
- 双相关筛选：以 PCC/SCC 双准入和双相关冗余约束获得稳定、互补的输入；
- MS-AgentNet：以 DSConv-S、RAA 和 DSConv-L 形成局部增强、全局交互和长尺度细化；
- 分层评价：检验域内跨电池表现、贡献来源、资源代价和跨域适应边界。

#### 第五级：贡献怎样成为可检验承诺

每项贡献必须包含：

> 现有问题 → 本文设计动作 → 与已有方法的实质区别 → 后文证据类型。

贡献不得写成实验章节目录，也不得用“首次、全面、彻底、确保”等优先权或保证性语言。第三项贡献应主动表达评价价值，不应以“为了避免数据泄漏质疑”或“为了保证公平可信”开头。

### 18.4 引言各类内容的颗粒度分级

#### A 级：必须深入

满足“本文独有设计、决定核心主张、需要后文验证”任一条件的内容，应写到审稿人能够明确判断缺口与贡献：

- 固定窗口跨电池不稳定的具体原因；
- 单一相关性无法同时覆盖线性、单调性和冗余问题；
- 局部细节、长期依赖与资源开销之间的矛盾；
- 高效注意力降低复杂度后仍需局部信息补偿；
- MS-CCCT、双相关筛选和 MS-AgentNet 相对已有工作的实质改变；
- 域内跨电池泛化与跨数据集迁移的概念边界；
- 三项挑战、三项贡献和后文证据的一一对应。

#### B 级：写到足以建立缺口

以下内容只需说明能力与限制，不展开完整原理：

- 电化学模型和 ECM；
- 传统机器学习；
- CNN、RNN、Transformer；
- 混合模型、线性注意力和稀疏注意力；
- IC、DV、DTV、时间特征和局部容量特征；
- 代表性前人工作。

每类通常采用 2—4 句：一句能力、一句代表工作、一句限制，必要时一句与本文问题的含义。

#### C 级：一笔带过

- 电池应用领域的完整罗列；
- 退化机理和安全事故链条；
- SOH、PCC、SCC 和标准模型的教材定义；
- 每篇文献的网络层数、全部数据集与全部结果；
- 公开数据集的一般权威性；
- 实验参数、阈值、窗口步长、训练比例和全部基线；
- 已由公式、图表或后文章节承担的执行细节。

#### D 级：原则上不进入引言

- 为预防假想质疑而增加的统计显著性、置信区间和不确定性分析；
- 与标题和贡献无关的硬件、LUT、实时调度或安全阈值自证；
- 对每个不占优结果的辩护；
- 证明阈值、初始化、模型深度“绝对正确”的内容；
- 无法进入任何“问题—设计—证据”闭环的旁支文献。

### 18.5 推荐的引言段落功能顺序

默认保留现有章节划分，在原结构内按以下功能检查段落；只有用户确认后才进行结构重排：

1. 应用与健康管理价值；
2. 容量型 SOH 及直接获取困难；
3. 提前提出输入稳定性与模型效率双重约束；
4. 模型驱动方法与传统机器学习的适用边界；
5. CNN、RNN、Transformer 的能力—限制递进；
6. 混合模型与高效注意力仍存的局部—全局效率矛盾；
7. 健康指标的噪声、固定窗口和跨电池稳定性问题；
8. 双相关筛选、冗余与数据域变化问题；
9. 综合缺口收束；
10. 三项挑战；
11. 方法总述；
12. 三项贡献。

段落之间必须单向推进，不得出现“模型问题 → 健康指标 → 再回模型问题”或“提出贡献 → 补背景 → 再重复贡献”的话题回摆。

### 18.6 顶刊式语言表述：先传递功能，再选择词汇

#### 18.6.1 陈述句优先：用事实顺序承担逻辑

BMSFormer 引言的清楚主要来自连续陈述，而不是依赖大量转折词、总结词或抽象名词。当前引言修改必须优先采用直接陈述句，让句子顺序本身呈现递进关系。

基本要求：

1. 一句话只陈述一个主要事实、能力、限制或设计动作；
2. 主语尽量明确，优先使用“某类方法能够……”“该结构需要……”“本文提出……”；
3. 能用动词表达时，不改写成复杂名词结构；
4. 前一句写能力，后一句直接写限制，不必每次添加“然而、但是、值得注意的是”；
5. 前一句写缺口，后一句直接写本文回应，不必反复使用“基于上述分析、针对上述问题、因此可以看出”；
6. 只有关系可能被误解时才使用转折或因果词；连接词承担真实逻辑，不承担段落装饰。

推荐的连续陈述：

> CNN 能够提取局部退化特征。其有限感受野难以直接建立远距离联系。RNN 通过隐藏状态传递历史信息，但串行递推限制了并行计算。Transformer 能够建立全局交互，标准自注意力的时间和存储开销随序列长度二次增长。

避免写成：

> 一方面，CNN 凭借其强大的局部特征提取能力实现了对局部退化信息的有效表征；另一方面，由于受到有限感受野这一固有结构特性的制约，其对于长期依赖关系的建模仍然存在一定局限性。此外，RNN 虽然能够……然而……与此同时……

#### 18.6.2 减少复杂名词化和空泛抽象词

优先使用具体动词：

- “提取局部特征”，不写“实现局部特征提取能力的增强”；
- “建立长程联系”，不写“实现长程依赖关系建模能力的提升”；
- “降低计算开销”，不写“实现计算资源使用效率的优化”；
- “筛除冗余指标”，不写“开展候选指标冗余性的筛选处理”；
- “窗口由多节电池共同确定”，不写“实现面向跨电池适用性的组级窗口确定机制”；
- “评价未见电池”，不写“开展针对未见电池泛化能力的综合评价工作”。

重点压缩以下结构：

- “对……进行……”；
- “实现……的提升/增强/优化”；
- “……能力/机制/体系/框架/过程/问题”连续叠加；
- “从……角度/层面来看”；
- “具有重要意义/发挥重要作用”；
- “在一定程度上能够有效……”；
- “进一步充分说明/全面验证……”。

术语只在其具有明确技术边界时保留。例如“跨电池稳定性”“长程依赖”“线性注意力复杂度”具有具体含义，应保留；“综合性、系统性、有效性、可靠性、严谨性”等自我评价词若不提供新事实，应删除。

#### 18.6.3 控制转折词和递进词密度

以下词语不是禁用，但同一段内原则上只保留真正改变逻辑方向的一处：

- 然而、但是、尽管、相比之下；
- 此外、同时、进一步、与此同时；
- 因此、由此、据此、综上所述；
- 值得注意的是、需要指出的是、不可忽视的是。

优先删除的装饰性连接词：

- 删除后前后关系仍然清楚；
- 只是提示读者“下面还有一句”；
- 连续两句分别以“然而”“此外”“因此”开头；
- 用“前者、后者、一方面、另一方面”代替具体对象；
- 段落末尾已经形成结论，却再用“综上所述”重复一遍。

连接关系优先通过以下方式呈现：

1. 保持“能力 → 限制 → 缺口 → 回应”的句序；
2. 重复必要的具体主语，不滥用“其、该方面、前者、后者”；
3. 用准确动词承接上一句；
4. 删除只负责提醒逻辑、却不增加事实的元话语。

#### 18.6.4 句长和句式边界

- 优先使用短而完整的陈述句，不使用电报式碎句；
- 一个句子一般不同时承担背景、文献、限制、因果和本文回应五种功能；
- 出现两个以上分号、三层以上“的”字结构或连续多个抽象名词时，优先拆句；
- 拆句后必须保持单向推进，不能用多个短句重复同一判断；
- 引言的语言目标是让审稿人一次读懂，不是通过复杂句式制造学术感。

#### 提出研究价值

- “准确的 SOH 估计是电池运行管理、寿命预测与安全决策的重要基础。”
- “该定义所需的最大可用容量难以在正常运行过程中直接获得，因此需要从可观测信号中间接估计。”
- “对于资源受限 BMS，估计方法还需要同时考虑输入可获得性、预测精度与资源开销。”

#### 归纳现有能力与限制

- “该类方法能够……，但其性能依赖于……。”
- “上述研究改善了……，然而……仍然限制其在……场景中的应用。”
- “混合结构增强了局部与长期信息的联合表征，但额外模块也增加了参数和计算开销。”
- “高效注意力降低了全局交互成本，但通常缺少显式的局部邻域偏置。”
- “依赖经验固定窗口的健康指标难以适应不同化学体系和运行协议下的退化响应差异。”

#### 收束研究缺口

- “现有研究分别改善了健康指标提取、局部特征建模或长程信息交互，但输入特征的跨电池稳定性与轻量局部—全局建模仍缺少统一设计。”
- “因此，关键问题不只是降低估计误差，而是在有限资源下保持稳定输入以及局部变化与长期趋势的协同表征。”

#### 引出本文设计

- “为此，本文从输入表征与序列建模两个层面展开设计。”
- “不同于依赖固定经验窗口的方法，本文将观测区间表述为组级标定变量。”
- “RAA 以少量智能体介导跨位置交互，避免形成完整的两两相关性矩阵。”
- “DSConv-S 与 DSConv-L 分别承担局部信息保留和长尺度趋势细化，与全局交互形成互补。”

#### 描述证据和边界

- “结果表明……”“结果显示……”“该结果支持……”
- “这一现象与……的设计目标一致，但不据此宣称唯一因果关系。”
- “参数量与存储结果体现出部署潜力，而非已经完成嵌入式部署验证。”
- “负 (R^2) 表明当前表示仍不足以解释目标域退化变化。”

### 18.7 防御性写作识别与替换

以下句型原则上删除或改写：

- “为保证公平性、严谨性和可信性……”
- “为了避免审稿人对数据泄漏产生质疑……”
- “虽然该结果不是最优，但并不影响……”
- “为了进一步证明该方法的有效性和合理性……”
- “考虑到该任务非常困难，因此该结果仍然可以接受……”
- “这充分证明/彻底验证/确保了……”

替换为可核查事实：

- “模型在训练电池上学习参数，并利用配置电池确定超参数；主对比按第四章规定范围报告结果。”
- “Transformer 在 Cell8 上误差最低；MS-AgentNet 的六电池宏平均误差最低。”
- “通过移除 DSConv 和 RAA，比较各模块及其组合的贡献。”
- “部分迁移方向的 (R^2) 仍为负，说明当前跨域表示尚不充分。”

防御性自证的判定问题：

1. 这句话是否主要在评价作者自己的严谨性，而不是陈述协议？
2. 这句话是否回应了尚未出现、且不影响核心结论的假想质疑？
3. 删除后，核心主张、复现信息和证据关系是否仍然成立？
4. 它是否会迫使后文新增与标题和贡献无关的实验线？

若第 1、2 或 4 项为“是”，且第 3 项为“是”，优先删除或改写。

### 18.8 何时才允许建议补充分析

只有以下情况才建议补充：

- 摘要、挑战或贡献中的核心主张没有对应证据；
- 当前实验无法区分健康指标贡献与模型贡献；
- 数据划分存在真实泄漏或会改变结论的比较不公平；
- “在线、轻量、部署”等措辞明显超出复杂度或硬件证据；
- 缺失决定复现的关键方法或训练信息。

不得仅因为“审稿人可能会问”而建议增加更多数据集、初始化策略、统计显著性、不确定性、MCU、置信区间或对每个非最优结果的补充实验。

### 18.9 当前引言审查的强制输出格式

每轮引言审查先按递进关系回答，不直接落入零散句子修改：

1. **审稿人认知链：** 当前各段依次让审稿人知道了什么，在哪里中断或回摆；
2. **价值传递判断：** 研究价值、具体缺口和本文回应是否依次建立；
3. **深度分级：** 标明哪些段落应深入、压缩、一笔带过或删除；
4. **最多五项高优先级问题：** 只列真正影响主线的问题；
5. **范文依据：** 分别说明 BMSFormer 与 SL-AgentNet 在对应位置采用了什么叙事动作；
6. **防御性筛查：** 列出自证、假想质疑、过强主张和可能诱发冗余补证的句子；
7. **原文—建议稿—效果差异：** 用户要求具体修改时，再按正文修改授权规则提供完整对照。

引言修改完成后，审稿人应能用一句话复述：

> 本文面向资源受限 BMS，通过组级自适应健康指标构建与轻量局部—全局序列建模，同时改善输入的跨电池稳定性和预测模型的资源效率，并通过分层实验评价其域内泛化、贡献来源、计算代价与跨域适应边界。

## 19. 范文学习、语言资产迁移与双向缺口审查

本节固化范文学习的执行原则。范文学习不是一般性的英文润色，也不是只总结“顶刊风格”，而是反向拆解同领域高水平论文如何选择信息、安排证据并建立审稿人认知。每次学习范文都必须同时回答：该领域通常如何叙事、某类内容写到什么级别、哪些内容深入、哪些内容一笔带过、作者如何让审稿人理解研究价值与亮点，以及哪些词语和句型可以迁移到本文。

### 19.1 必须拆解的五个层面

1. **价值链：** 领域背景如何收束为具体问题，现有方法的不足如何产生实际影响，本文为何值得单独研究；
2. **创新链：** 以前能做到什么，本文进一步改变了什么，创新来自数据、指标、方法、机制、评价还是应用场景；
3. **证据链：** 亮点如何经过“提出—实现—比较—解释—限定”形成闭环；
4. **颗粒度：** 哪些内容写公式、数据、比较、原因与边界，哪些只需一句交代，哪些不进入正文；
5. **语言链：** 术语、固定搭配、功能短语、句型骨架与段落推进分别承担什么论证功能。

不得只摘抄“漂亮句子”。每个表达都要回到它在原文中的位置、对象、证据强度和叙事任务。

### 19.2 可直接迁移的词语级资产

范文中的标准技术术语、领域通用搭配和常见功能短语可以直接用于本文，但必须保证研究对象、语义范围和证据强度一致。完整长句或具有作者独特表达特征的连续表述不得原样复制；可保留其句型骨架并替换为本文事实。

每次建立表达库时至少记录：

| 字段 | 要求 |
| --- | --- |
| 原始表达 | 范文中的术语、搭配、短语或句型骨架 |
| 精确含义 | 它在电池 SOH/BMS 语境中实际表示什么 |
| 论证功能 | 提出问题、归纳能力、指出限制、引出方法、报告结果、解释现象、限定边界或强调价值 |
| 使用位置 | 摘要、引言、方法、结果、讨论或结论 |
| 证据强度 | 事实描述、结果指示、合理推断、机制支持或严格证明 |
| 迁移等级 | 可直接使用、替换研究对象后使用、只学习结构、禁止照搬 |
| 本文落点 | 对应章节、段落及具体内容 |
| 风险 | 是否会造成过强主张、概念混用、贡献越界或证据不足 |

提取必须覆盖四种颗粒度：

1. **专业术语：** 对任务、现象、性能、数据角色和适用范围的标准命名；
2. **固定搭配：** 领域中稳定的动词—宾语、形容词—名词组合；
3. **功能短语：** 研究缺口、方法动作、结果比较、原因解释和边界限定的常见表达；
4. **句型骨架：** 范文用于组织“能力—限制”“问题—设计”“现象—证据—意义”的通用结构。

词汇选择必须服从语义精度，不能只追求“高级”。需要逐项区分近义词的动作对象和证据等级，例如：

- capture、characterize、extract、identify 分别对应表征、刻画、提取和识别；
- improve、enhance、reduce、mitigate 对应不同变化方向与对象；
- robustness、stability、generalizability、adaptability 不得互换；
- degradation pattern、degradation trajectory、aging dynamics 对应不同分析层级；
- suggests、indicates、shows、supports、demonstrates 必须根据证据强度选择。

### 19.3 正向陈述优先，防御性表达只在必要处使用

正文默认采用正向、直接、可核查的陈述句。句子首先告诉审稿人“发生了什么、本文做了什么、证据说明什么”。只有在排除真实歧义、限定结论范围、报告负结果或回应会改变核心结论的问题时，才使用防御性或让步表达。

不得把“不是……而是……”“尽管……但不影响……”“为了证明合理性……”等结构当作顶刊句型重复使用。范文中的防御性措辞也要区分正常正文、修回补证和审稿回复痕迹；后两类不自动迁移到本文。

### 19.4 深入与简写必须由论证功能决定

应深入分析：直接支撑核心创新的设计、关键定量结果、主要基线比较、性能来源、异常或负结果、适用边界及决定可复现性的细节。

应一笔带过：领域共识、标准方法与指标、表格或公式已经表达的内容、与核心贡献关系较弱的参数细节、不会改变结论的补充结果。

判断依据不是“范文写了多少”，而是该内容在范文中承担什么功能。范文深入写某项内容，可能因为它属于该文核心贡献；本文只有提出同等级主张时，才需要匹配相应的分析深度和证据负担。

### 19.5 双向内容缺口审查

完成范文学习后，必须对范文与本文进行双向检查，不能只查“本文少了什么”。按以下四类输出：

| 对照结果 | 必须判断的问题 |
| --- | --- |
| 范文有、本文没有 | 是缺少必要问题、方法、证据或讨论，还是研究范围不同而无需补充 |
| 本文有、范文没有 | 是本文独特贡献，还是过度解释、重复说明、旁支内容或防御性补证 |
| 双方都有 | 位置、篇幅、术语、颗粒度和结论强度是否对齐 |
| 双方都没有 | 是否确属本研究无需覆盖的内容，避免为了表面完整而硬加 |

每项差异必须标记为：建议补充、建议保留、建议压缩、建议删除、因研究边界不同无需对齐、需要导师/师兄确认。不得因范文存在某项内容就自动建议本文增加。

### 19.6 独立 agent 的范文缺口复核

按组审阅章节时，除一般性的建议稿复核外，必须安排独立 agent 进行只读的双向缺口检查。该 agent 不修改正文，重点查找主审可能因熟悉本文而忽略的内容差异：

1. 范文承担核心论证功能、本文却没有对应内容的部分；
2. 本文篇幅明显超过范文且未增加新证据或新价值的解释；
3. 范文使用标准领域术语、本文却采用自造或边界模糊表达的部分；
4. 本文主张强度高于范文但证据颗粒度更低的部分；
5. 因研究对象、数据、贡献或目标场景不同而不应机械对齐的部分。

主审与独立 agent 意见不一致时，保留分歧及各自依据，由用户决定是否补充、压缩或保持原状。

### 19.7 范文来源与贡献边界

- BMSFormer 属于导师的 BMS 工作，主要用作资源受限 BMS 场景、精度—效率矛盾和论文整体价值链的叙事基准；
- SL-AgentNet 属于师兄工作，主要用作局部—全局模块分工、退化现象组织和工程证据层级的参考；
- 第三篇范文如用于模型综述，只学习与本文直接相关的功能分工、轻量化表达和段落推进，不继承其不同任务设定与技术路线；
- 不得因为学习范文而把导师或师兄的具体贡献改写成本研究原创，也不得添加本文没有实验或理论支持的机理、性能或应用主张。

论文正文禁止使用“沿用某某设计”“参考某某框架”“借鉴某某方法”等以他人工作为主语中心的来源式表述，也不得用导师、师兄或作者姓名解释本文结构来源。正文应像两篇范文一样，正向陈述本文设计了什么模块、构建了什么数据流、各操作承担什么功能，并在相应技术事实或相关工作后正常标注文献。

这一禁令覆盖所有方法与实验元话语，包括“参考已有方法构造”“参考文献的特征构造方式”“在某模型思路的基础上”“定义参照文献”“基线结构参考某模型设置”等。遇到此类句子，直接陈述本文构造了什么指标、采用了什么搜索、如何定义指标、基线如何配置，并将文献引用放在对应技术事实之后。数据“来源于某机构”、实验中的“参考电芯”以及“继续采用本文前述协议”不属于来源式写作，但若“沿用”只表示本文内部承接，优先改为“继续采用”或“遵循”以减少歧义。

“所设计、所提出、所构建”可以用于本文确定并命名的模块结构、参数配置、计算路径及其组合，不要求在方法章逐项声明其中每个基础算子是否原创。标准卷积、DSConv、注意力、ReLU 等通用操作仍按通用名称陈述；由本文配置形成的 DSConv-S、DSConv-L、RAA、SLFA 和 MS-AgentNet 等模块可以根据上下文使用“本文设计/提出/构建”。只避免“首次、原创、全新、独创”等需要优先权证据的表述，不把语言修改变成贡献来源审计。

### 19.8 范文学习的必备交付物

每轮系统性范文学习至少形成：

1. 同领域顶刊共同叙事规律；
2. 研究价值与创新点的审稿人认知链；
3. 分章节的深入、简写和不写清单；
4. 可直接迁移的术语、固定搭配、功能短语和句型骨架库；
5. 近义词语义边界与结论强度表；
6. 范文—本文双向缺口表；
7. 防御性、过度解释和过强主张清单；
8. 对本文具体段落的落点建议，但正文仍遵守“先审后改、用户确认后落盘”。

## 图表英文题名：用户确认并已落实

图表题名以BMSFormer对应图表的句式和详略逐项模仿：范文只给总题则保留总题，范文为区分内容列分图时按本文实际对象列出，不额外增加机制、效果或协议说明，不按助手自定的统一短题规则扩写/缩写。当前启用10图、19表题名均为英文。用户确认后已修改Fig. 2、4、5、7、10和Table 7，其他23处保持；短caption同步，不编译。图内标签与表内内容不属本轮caption修改。详见 D:/初稿/修改/图表英文题名/按BMS逐图对应_题名修改清单.md 及 English_captions_current.md。旧16项/4项候选不再执行。
